import { Ng2EchartsDirective } from './ng2-echarts.directive';

describe('Ng2EchartsDirective', () => {
  it('should create an instance', () => {
    const directive = new Ng2EchartsDirective();
    expect(directive).toBeTruthy();
  });
});
